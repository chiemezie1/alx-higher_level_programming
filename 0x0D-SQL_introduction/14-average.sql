-- A script that computes the score average of all records in
--     the table second_table of the database in your MySQL server

-- The result column name should be average

SELECT AVG(score) AS average 
FROM second_table